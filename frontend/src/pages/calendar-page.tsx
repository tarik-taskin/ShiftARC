import { CalendarCog, ChevronLeft, ChevronRight, RotateCcw, Save } from 'lucide-react'
import { useMemo, useState } from 'react'

import { useCalendarOverrides, useDeleteCalendarOverride, useSaveCalendarOverride } from '@/api/calendar/queries'
import { useDayTypes } from '@/api/day-types/queries'
import { Button } from '@/components/ui/button'
import { Select } from '@/components/ui/select'

const weekdays = ['Pzt', 'Sal', 'Çar', 'Per', 'Cum', 'Cmt', 'Paz']

export function CalendarPage() {
  const [month, setMonth] = useState(() => { const now = new Date(); return new Date(now.getFullYear(), now.getMonth(), 1) }); const [selected, setSelected] = useState<string | null>(null); const [dayTypeId, setDayTypeId] = useState(''); const [today] = useState(() => iso(new Date()))
  const from = iso(month); const next = new Date(month.getFullYear(), month.getMonth() + 1, 1); const to = iso(new Date(next.getTime() - 86_400_000))
  const overrides = useCalendarOverrides(from, to); const dayTypes = useDayTypes(false); const save = useSaveCalendarOverride(); const remove = useDeleteCalendarOverride()
  const overrideMap = useMemo(() => new Map(overrides.data?.map((item) => [item.date, item]) ?? []), [overrides.data]); const selectedOverride = selected ? overrideMap.get(selected) : undefined

  function select(date: string) { if (date < today) return; setSelected(date); setDayTypeId(overrideMap.get(date)?.dayTypeId ?? '') }

  return <div className="space-y-8 pt-4 sm:pt-8"><section><p className="section-kicker">Şablonun istisnaları</p><h1 className="mt-4 text-4xl font-semibold tracking-[-0.05em] sm:text-6xl">Planlama takvimi</h1><p className="mt-4 max-w-2xl text-muted-foreground">Belirli bir tarihte haftalık şablondan farklı gün tipi kullan. Snapshot üretildikten sonra seçim değiştirilemez.</p></section><section className="grid gap-6 xl:grid-cols-[1fr_360px]"><div className="rounded-3xl border bg-card/55 p-4 sm:p-6"><div className="flex items-center justify-between"><Button variant="ghost" size="sm" aria-label="Önceki ay" onClick={() => { setMonth(new Date(month.getFullYear(), month.getMonth() - 1, 1)); setSelected(null) }}><ChevronLeft className="size-4" /></Button><h2 className="text-lg font-semibold">{new Intl.DateTimeFormat('tr-TR', { month: 'long', year: 'numeric' }).format(month)}</h2><Button variant="ghost" size="sm" aria-label="Sonraki ay" onClick={() => { setMonth(next); setSelected(null) }}><ChevronRight className="size-4" /></Button></div><div className="mt-5 grid grid-cols-7 gap-1">{weekdays.map((day) => <div key={day} className="py-2 text-center text-xs font-semibold text-muted-foreground">{day}</div>)}{cells(month).map((date, index) => date ? <button key={date} disabled={date < today} onClick={() => select(date)} aria-label={`${date} gününü düzenle`} className={`min-h-20 rounded-xl border p-2 text-left ${selected === date ? 'border-primary bg-primary/15' : 'border-border bg-background/40'} disabled:opacity-30`}><span className="text-xs font-semibold">{Number(date.slice(-2))}</span>{overrideMap.get(date) ? <div className="mt-3 rounded-lg px-2 py-1 text-[10px] font-semibold text-white" style={{ backgroundColor: overrideMap.get(date)!.dayTypeColor }}>{overrideMap.get(date)!.dayTypeName}</div> : null}</button> : <div key={`empty-${index}`} />)}</div></div><aside className="rounded-3xl border bg-card/55 p-5">{selected ? <><p className="text-xs font-semibold uppercase tracking-wider text-primary">{new Intl.DateTimeFormat('tr-TR', { dateStyle: 'long' }).format(new Date(`${selected}T12:00:00`))}</p><h2 className="mt-2 text-xl font-semibold">Gün tipi istisnası</h2><label className="mt-6 block text-sm">Gün tipi<Select value={dayTypeId} onValueChange={setDayTypeId} ariaLabel="Gün tipi" options={[{ value: '', label: 'Gün tipi seç' }, ...(dayTypes.data?.map((type) => ({ value: type.id, label: type.name })) ?? [])]} /></label>{(save.error || remove.error) ? <p role="alert" className="mt-3 text-sm text-destructive">{(save.error ?? remove.error)?.message}</p> : null}<div className="mt-5 flex gap-2"><Button disabled={!dayTypeId || save.isPending} onClick={() => save.mutate({ date: selected, dayTypeId, version: selectedOverride?.version ?? null })}><Save className="size-4" />Kaydet</Button>{selectedOverride ? <Button variant="outline" onClick={() => remove.mutate({ date: selected, version: selectedOverride.version })}><RotateCcw className="size-4" />Şablona dön</Button> : null}</div></> : <div className="grid min-h-64 place-items-center text-center"><div><CalendarCog className="mx-auto size-9 text-primary" /><p className="mt-4 font-semibold">Gelecek bir gün seç</p><p className="mt-2 text-sm text-muted-foreground">Haftalık plandan farklı olacak günleri burada belirle.</p></div></div>}</aside></section></div>
}

function iso(date: Date) { return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}` }
function cells(month: Date): Array<string | null> { const first = (month.getDay() + 6) % 7; const count = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate(); return [...Array.from({ length: first }, () => null), ...Array.from({ length: count }, (_, i) => iso(new Date(month.getFullYear(), month.getMonth(), i + 1)))] }
